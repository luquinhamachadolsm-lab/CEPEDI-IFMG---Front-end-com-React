class Conta {
    #saldo
    constructor(saldo) {
        this.#saldo = saldo
    }
    depositar(){
        return a = Number(prompt("quanto deseja depositar?"))
        a + saldo == this.#saldo
    }
    consultar(){
        return console.log(`Seu saldo é de:${this.#saldo}`)
    }
    sacar(){
        if (b <= saldo) {
            b - saldo == this.#saldo
        }
        return b = Number(prompt("quanto deseja sacar?"))
    }
}